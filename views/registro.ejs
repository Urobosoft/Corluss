<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://kit.fontawesome.com/64d58efce2.js" crossorigin="anonymous"></script>

  <link rel="shortcut icon" href="/img/icono.png"" type=" image/x-icon">
  <link rel="stylesheet" href="/css/login.css">

  <title>Registrate</title>
</head>

<body>
  <div class="container sign-up-mode">
    <div class="forms-container">
      <div class="signin-signup">
        <form id="formLogin" class="sign-in-form" action="/Corluss/login" method="POST">
          <h2 class="title">Inicio de sesión</h2>
          <div class="input-field">
            <i class="fas fa-user"></i>
            <input type="text" name="nomUsuario" placeholder="Nombre de usuario" />
          </div>
          <div class="input-field">
            <i class="fas fa-lock"></i>
            <input type="password" name="password" placeholder="Contraseña" id="password2" />
            <i class="fas fa-eye" id="togglePasswor"></i>
          </div>
          <a href="#" id="forgotPasswordLink" class="forgot-password">Olvidé mi contraseña</a>
          <button class="w-100 btn btn-lg btn-primary" class="btn solid" type="submit">Iniciar sesión</button>

          <p class="social-text">Inicie sesión en plataformas sociales</p>

          <div class="social-media">
            <a href="#" class="social-icon">
              <i class="fab fa-facebook-f"></i>
            </a>
            <a href="#" class="social-icon">
              <i class="fab fa-twitter"></i>
            </a>
            <a href="#" class="social-icon">
              <i class="fab fa-google"></i>
            </a>
          </div>
        </form>


        <form action="/Corluss/register" id="formRegistro" class="sign-up-form" method="POST" enctype="multipart/form-data">
          <h2 class="title">Regístrate</h2>
          <div class="form-columns">
            <div class="form-column">
              <div class="input-field">
                <i class="fas fa-user"></i>
                <input type="text" placeholder="Nombre" name="nombre" value="<%= valoresAnteriores.nombre %>" />
              </div>
              <% if (errorN) { %>
                <div class="error-message">
                  <%= errorN %>
                </div>
                <% } %>

                  <div class="input-field">
                    <i class="fas fa-user"></i>
                    <input type="text" placeholder="Apellido" name="apellido"
                      value="<%= valoresAnteriores.apellido %>" />
                  </div>
                  <% if (errorA) { %>
                    <div class="error-message">
                      <%= errorA %>
                    </div>
                    <% } %>

                      <div class="input-field">
                        <i class="fas fa-user"></i>
                        <input type="text" placeholder="Nombre de usuario" value="<%= valoresAnteriores.nomUsuario %>"
                          name="nomUsuario" />
                      </div>

                      <% if (errorU) { %>
                        <div class="error-message">
                          <%= errorU %>
                        </div>
                        <% } %>

                          <div class="input-field">
                            <i class="fas fa-envelope"></i>
                            <input type="text" placeholder="Número de celular o correo electrónico"
                              value="<%= valoresAnteriores.contacto %>" name="contacto" />
                          </div>
                          <% if (errorC) { %>
                            <div class="error-message">
                                <%= errorC %>
                            </div>
                        <% } %>
                        
                        <% if (errorD) { %>
                            <div class="error-message">
                                <%= errorD %>
                            </div>
                        <% } %>
                        


                              <div class="input-field">
                                <i class="fas fa-lock"></i>
                                <input type="password" id="password" placeholder="Contraseña" name="password"
                                  value="<%= valoresAnteriores.password %>" />
                                <i class="fas fa-eye" id="togglePassword"></i>
                              </div>

                              <span id="passwordError" class="error"></span>

            </div>

            <div class="form-column">
              <div class="input-field">
                <i class="fas fa-venus-mars"></i>
                <label for="genero">Género</label>
              </div>

              <select name="genero">
                <option value="M" <%=valoresAnteriores.genero==='M' ? 'selected' : '' %>>Masculino</option>
                <option value="F" <%=valoresAnteriores.genero==='F' ? 'selected' : '' %>>Femenino</option>
                <option value="O" <%=valoresAnteriores.genero==='O' ? 'selected' : '' %>>Otro</option>
              </select>

              <div class="input-field">
                <i class="fas fa-calendar-alt"></i>
                <label for="fecha-nacimiento">Fecha de Nacimiento</label>
              </div>

              <div class="form-columns">
                <div class="form-column">
                  <label>Día:</label>
                  <br>
                  <select id="dia" name="dia"></select>
                </div>

                <div class="form-column">
                  <label>Mes:</label>
                  <br>
                  <select id="mes" name="mes"></select>
                </div>

                <div class="form-column">
                  <label>Año:</label>
                  <br>
                  <select id="año" name="ano"></select>
                </div>
              </div>

              <script>
                var valoresAnteriores = {
                  dia: <%= valoresAnteriores.dia %>,
                  mes: <%= valoresAnteriores.mes %>,
                  ano: <%= valoresAnteriores.ano %>
                };
              </script>

              <input type="button" id="btn-abrir-popup" class="boton" value="Foto de perfil" />

              <% if (errorE) { %>
                <div class="error-message">
                  <%= errorE %>
                </div>
                <% } %>


                  <div class="overlay" id="overlay">
                    <div class="popup" id="popup">
                      <div class="image-label-container">
                        <p class="text">Foto de perfil</p>
                        <a href="#" id="btn-cerrar-popup" class="btn-cerrar-popup"><i class="fas fa-times"></i></a>

                        <% if(valoresAnteriores.foto) { %>
                          <img src="<%= valoresAnteriores.foto %>" alt="avatar" id="img" />
                          <% } else { %>
                            <img src="/img/perfil.png" alt="avatar" id="img" />
                            <% } %>
                              <label for="foto">SUBIR FOTO</label>
                              <input type="file" name="image" id="foto" accept="image/*" />

                      </div>
                    </div>
                  </div>

            </div>
          </div>
          <button type="submit" class="btn sz olid">Registrarse</button>

          <div>
            <p class="social-text">Regístrate con plataformas sociales</p>
            <div class="social-media">
              <a href="#" class="social-icon">
                <i class="fab fa-facebook-f"></i>
              </a>
              <a href="#" class="social-icon">
                <i class="fab fa-twitter"></i>
              </a>
              <a href="#" class="social-icon">
                <i class="fab fa-google"></i>
              </a>
            </div>
          </div>
        </form>

      </div>
    </div>

    <div class="panels-container">
      <div class="panel left-panel">
        <div class="content">
          <h3>¿Eres nuevo usuario?</h3>
          <p>
            Regístrate en nuestra plataforma para acceder a los contenidos de la
            aplicación
          </p>
          <button class="btn transparent" id="sign-up-btn">
            Regístrate
          </button>
        </div>
        <img src="/img/log.svg" class="image" alt="" />
      </div>
      <div class="panel right-panel">
        <div class="content">
          <h3>¿Ya tienes cuenta?</h3>
          <p>
            Como usuario registrado, puedes acceder a tu cuenta y disfrutar de
            todos nuestros servicios.
          </p>
          <button class="btn transparent" id="sign-in-btn">
            Inicia sesión
          </button>
        </div>
        <img src="/img/register.svg" class="image" alt="" />
      </div>
    </div>
  </div>

  <div id="forgotPasswordPopup" class="rep" style="display: none;">
    <div class="rep-content">
      <span class="close-popup">&times;</span>
      <form id="forgotPasswordForm">
        <!-- Contenido del formulario, por ejemplo, un campo de entrada para el correo electrónico -->
        <input type="email" placeholder="Introduce tu correo electrónico" required>
        <input type="submit" value="Mandar">
      </form>
    </div>

    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <% if (typeof alert !=='undefined' && alert) { %>
      <script>
        Swal.fire({
          title: '<%= alertTitle %>',
          text: '<%= alertMessage %>',
          icon: '<%= alertIcon %>',
          showConfirmButton: <%= showConfirmButton %>,
          timer: <%= timer %>
        }).then(() => {
            window.location = '/<%= ruta %>'
          })
      </script>
      <% } %>


        <script src="/js/registro.js"></script>
        <script src="/js/popup.js"></script>
</body>

</html>